# Loaded last. Do not edit the image-owned defaults.
# Changing a binding requires removing the default first, for example:
# unbind = SUPER, Return
# bind = SUPER, Return, exec, foot --app-id=personal-terminal
# Input overrides can also go here:
# input { kb_layout = us }
