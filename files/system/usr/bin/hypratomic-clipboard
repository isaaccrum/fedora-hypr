#!/usr/bin/env bash
set -euo pipefail

selection="$(cliphist list | walker --dmenu --placeholder 'Clipboard')" || exit 0
[[ -n "$selection" ]] || exit 0
printf '%s\n' "$selection" | cliphist decode | wl-copy
